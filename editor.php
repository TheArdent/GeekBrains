<h2>Консоль редактора</h2><?phpinclude_once('startup.php');include_once('model.php');// Установка параметров, подключение к БД, запуск сессии.startup();// Извлечение статей.$articles = articles_all();// Кодировка.header('Content-type: text/html; charset=utf-8');// Вывод в шаблон.$title = "Консоль редактора";// Внутренний шаблон.$content = view_include('view/v_editor.php',array('title' => $title,'articles' => $articles));// Внешний шаблон.$page = view_include(	'view/v_main.php',	array('title' => $title, 'content' => $content));// Вывод.echo $page;