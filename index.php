<h2>Главная</h2><?phpinclude_once('startup.php');include_once('model.php');// Установка параметров, подключение к БД, запуск сессии.startup();// Извлечение стать.$articlesIntro = articles_intro();// Кодировка.header('Content-type: text/html; charset=utf-8');$title = "Главная";// Внутренний шаблон.$content = view_include('view/v_index.php',array('title' => $title,'articlesIntro' => $articlesIntro));// Внешний шаблон.$page = view_include(	'view/v_main.php',        array('title' => $title, 'content' => $content)    );// Вывод.echo $page;